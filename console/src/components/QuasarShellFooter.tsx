export default function QuasarShellFooter() {
  return (
    <footer className="shell-disclaimer" aria-label="Operator disclaimer">
      Operator view over the trust gateway. Verdicts are backend-computed and signed —
      this console does not decide clearance.
    </footer>
  );
}
